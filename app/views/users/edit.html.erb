<h1>Editing User</h1>

<% if @password_status != 'WRONG' %>

<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>


<% else %>

  <%= form_for(@user, url: '/users/'+@user.id.to_s+'/edit/',method: :get) do |f| %>

    <div class="field">
      <%= f.label :password %><br>
      <%= text_field_tag :password %>
    </div>
     <div class="actions">
    <%= f.submit %>
  </div>

  <% end %>


<% end %>


